// Load verified configuration and resolve only required table or view objects.
// const config = input.config();
// const secretNames = {
//   apiToken: 'Verified Airtable secret name'
// };
// const secrets = Object.fromEntries(
//   Object.entries(secretNames).map(([key, name]) => [key, input.secret(name)])
// );

// Query only the fields used by this script.

// Validate run-wide and record-level invariants before side effects.

// Perform external operations and Airtable mutations safely.

// Report a concise, non-sensitive result.
